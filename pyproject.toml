[project]
name = "pide"
version = "0.1.0"
description = "Python IDE for learning/education"
requires-python = ">=3.10"
dependencies = [
    "PySide6>=6.6.0",
]

[project.scripts]
pide = "pide.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
